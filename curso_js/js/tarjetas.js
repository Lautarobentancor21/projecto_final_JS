 class Tarjeta {
     constructor(number, name, mes, year, ccv) {
         this.number = number;
         this.name = name;
         this.mes = mes;
         this.year = year;
         this.ccv = ccv;

     }
 }